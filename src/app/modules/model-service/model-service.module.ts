import { NgModule } from '@angular/core';
import { IonicModule } from 'ionic-angular';
import { ModelSvc } from './model.service';

@NgModule({
  imports: [IonicModule],
  providers: [ModelSvc]
})
export class ModelSvcModule {}
